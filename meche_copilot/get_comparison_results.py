import asyncio
from meche_copilot.esd_toolkit.schemas import Esd

async def get_comparison_results(esd_info: Esd):
    # TODO - call agent_exec_chain with prompt template and data for reading submittals
    await asyncio.sleep(20)